# the docker image needs to be downloaded, and copied to each of the k3s hosts
docker save -o ./website.tar website:latest

# load the created tar into docker 
sudo docker load -i ./website.tar

# push it to local registry
docker tag website:latest localhost:5000/website:latest
docker push localhost:5000/website:latest

